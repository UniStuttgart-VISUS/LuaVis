#ifndef FONT_SYMBOLS_HPP
#define FONT_SYMBOLS_HPP

#include <string>

namespace symSmall
{
extern const std::string

    arrowDown,
    arrowUp, arrowLeft, arrowRight,

    check, cross,

    maximize, unmaximize, rollUp, rollDown,

    refresh, create, open, save, saveAs, remove;
}

namespace symLarge
{
extern const std::string

    arrowDown,
    arrowUp, arrowLeft, arrowRight,

    check, cross,

    maximize, unmaximize, rollUp, rollDown,

    refresh, create, open, save, saveAs, remove,

    undo, redo,

    config, properties,

    zoomIn, zoomOut,

    toBottom, toTop,

    compile,

    paintSymbols;
}

#endif
